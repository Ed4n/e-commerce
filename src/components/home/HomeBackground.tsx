import React from 'react'

type Props = {}

export default function HomeBackground({ }: Props): JSX.Element {
    return (
        <div className='w-full h-screen bg-gray-300 fixed lg:static top-0 flex justify-center items-center'>HomeBackground</div>
    )
}